import * as React from "react";
import Button from "../Button";
import NativeBaseProvider from "../../NativeBaseProvider/NativeBaseProvider";

export default (
  // <NativeBaseProvider uxpId="base">
   <Button
     uxpId="button"
     disabled={false}>Let's Merge!</Button>
  // </NativeBaseProvider>
);
